/**
 * BMB Editor shared - hook globali per l'editor blocchi.
 */
( function ( wp ) {
	if ( ! wp || ! wp.hooks ) return;
	// Reservato per filtri condivisi futuri (es: aggiungere classi a tutti i blocchi BMB).
} )( window.wp );
